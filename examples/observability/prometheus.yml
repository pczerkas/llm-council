# Prometheus scrape config for the observability overlay (ADR-011 Phase 2).
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: llm-council
    static_configs:
      - targets: ["llm-council:9464"]
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
